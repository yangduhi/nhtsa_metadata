# 2026-05-03 | Stage J | COMPLETE | Schema v1.6 Evidence Lineage Model Result

## Purpose

Stage J implements the schema v1.6 operating surface for tracing each row from
source payload evidence through normalized features, candidate rule or final
disposition evidence, and final classification/disposition.

## Branch and workspace

- Branch: `codex/stage-h-to-v1-completion`
- Workspace: `D:\vscode\nhtsa_metadata`
- Registered worktrees: one canonical worktree only.
- New worktree created: no.

## Schema v1.6 tables

The schema v1.6 lineage surface includes:

- `test_classification`
- `test_classification_candidates`
- `classification_evidence`
- `classification_adjudication`
- `canonical_label_registry`
- `rule_registry`
- `program_standard_evidence`
- `test_event_domain`
- `impact_device_evidence`
- `restraint_equipment_evidence`
- `classification_feature_evidence`

The Alembic migration is:

- `alembic/versions/0004_schema_v1_6_evidence_lineage.py`

## Evidence-lineage flow

The operating flow is:

`source payload -> normalized feature -> candidate rule -> final classification or final disposition`

`classification_evidence` is the durable DB evidence table. It remains distinct
from:

- classifier runtime evidence produced during rule evaluation;
- CSV fixtures under `tests/fixtures/classification`;
- audit/report attachments under `docs/phase_reports`.

## v1.6 audit fixture

Generated by `scripts/build_classification_lineage_v1_6_audit.py`:

- `tests/fixtures/classification/classification_lineage_audit_v1_6.csv`

The audit fixture checks that every v1.4.2 accounted row has:

- source payload links;
- normalized feature evidence;
- candidate rule evidence or disposition adjudication evidence;
- final decision evidence.

## Lineage metrics

Using `classification_lineage_audit_v1_6.csv`:

- `total_count = 3891`
- `source_payload_linked_count = 3891`
- `normalized_feature_linked_count = 3891`
- `candidate_or_disposition_linked_count = 3891`
- `final_decision_linked_count = 3891`
- `complete_lineage_count = 3891`
- `missing_lineage_count = 0`

## Tests added

- `tests/test_classification_lineage_v1_6.py`

## Validation

Stage J validation passed:

- `pytest -q`: passed, `130 passed, 2 warnings`.
- `ruff check .`: passed.
- `mypy src\nhtsa_metadata`: passed, `50 source files`.
- `powershell -ExecutionPolicy Bypass -File scripts\verify.ps1`: passed.
- `powershell -ExecutionPolicy Bypass -File .harness\run.ps1`: passed.

## Hard constraint compliance

- No live NHTSA API calls were made.
- No raw source payloads were modified.
- No production data was rewritten.
- No push was performed.
- No merge to `main` was performed.
- No worktree or sibling repo folder was created.
