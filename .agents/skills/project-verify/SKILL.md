# Project Verify

Use this project skill before closing a phase.

1. Run `scripts/verify.ps1`.
2. Run `.harness/run.ps1`.
3. Confirm no live NHTSA call is required.
4. Record pass/fail results in `docs/phase_reports/`.
